[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  SECRETS_SCAN_OMIT_KEYS = "GMAIL_FROM_NAME,NEXT_PUBLIC_APK_URL,NEXT_PUBLIC_APP_URL,NEXT_PUBLIC_APK_URL"
  SECRETS_SCAN_OMIT_PATHS = "README.md,SETUP.md,.gitignore,.netlify"